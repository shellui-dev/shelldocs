---
title: Introduction
description: What ShellDocs is, and why it exists.
order: 1
---

# Introduction

**ShellDocs is a docs framework for .NET, like fumadocs for Node/React.**

Every .NET UI library ends up hand-rolling their own docs site. MudBlazor, Radzen, AvaloniaUI — each spent months rebuilding a sidebar, a search box, a code block, a theme toggle, from scratch. None of it is reusable.

ShellDocs is the "just use this" answer.

## What you get

- Markdown authoring with YAML frontmatter and inline Razor tags
- File-based routing over a `content/` folder
- Cmd+K search with a build-time client-side index
- Real Blazor components rendered inline, not iframes
- Composable with any Blazor component library
- Tailwind + shadcn aesthetic, matching ShellUI
- Animated with `prefers-reduced-motion` awareness
- Static site output — deploy anywhere

## Next

Head to [Installation](/docs/installation) to spin up your first ShellDocs site.
